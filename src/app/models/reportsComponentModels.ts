export interface TableColumn {
  field: string;
  header: string;
}

export interface DataSource {
  code: string;
  name: string;
}

export interface DataCurrency {
  code: string;
  name: string;
}

